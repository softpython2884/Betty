import { AppShell } from "@/components/layout/AppShell"

export default function ManagerPage() {
  return (
    <AppShell>
      <div>
        <h1 className="text-4xl font-headline tracking-tight">Manager Dashboard</h1>
        <p className="text-muted-foreground mt-2">Oversee all professors and academy progress.</p>
      </div>
    </AppShell>
  )
}
